

function ButtonOne({styles}) {
  return (
    <button type='submit' className={`py-4 px-6 bg-blue-gradient rounded-[8px] font-poppins font-medium text-[18px] text-primary outline-none ${styles}`}>Get OnBoard</button>
  )
}

export default ButtonOne;